# Social Skills v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build three Claude Code skills (`/post`, `/log`, `/audit`) that help Louis write, log, and audit LinkedIn + X posts in his voice, plus the `~/42/social/` content repo they read from.

**Architecture:** Skills live at `~/.claude/skills/<name>/SKILL.md` (user-level, behavior only). Content lives in a separate git repo at `~/42/social/` (env var `$SOCIAL_HOME`). Skills do prompting + file I/O via the `Read/Write/Edit/Glob/AskUserQuestion` tools — no Python helpers in v1. Manual publication, manual metric entry. Pattern engine deferred to v2.

**Tech Stack:** Markdown (skill files + content), YAML frontmatter, flat YAML data store, bash for repo setup, `yq` for YAML validation (already installed), `pytest` not required (skills are not unit-tested in v1).

**Spec:** `docs/superpowers/specs/2026-04-16-social-skills-design.md`

---

## File Structure

**New content repo** (`~/42/social/`):
```
~/42/social/
├── README.md                        # what lives here, how it ties to skills
├── .gitignore                       # ignore linkedin-export/ and drafts/.local
├── voice/
│   ├── linkedin.md                  # generated by /audit linkedin
│   └── x.md                         # generated by /audit x
├── sources/
│   ├── linkedin-posts.md            # seeded with 3 pasted posts
│   ├── linkedin-export/             # gitignored, archive when it arrives
│   └── x-brief.md                   # Louis's X voice brief
├── drafts/.gitkeep
├── posted/.gitkeep
├── data-store.yaml                  # empty list initially
└── evolution-log.md                 # header only initially
```

**New skill files:**
```
~/.claude/skills/post/SKILL.md       # /post linkedin|x
~/.claude/skills/log/SKILL.md        # /log linkedin|x
~/.claude/skills/audit/SKILL.md      # /audit linkedin|x
```

**Modified:**
- `~/.zshrc` — append `export SOCIAL_HOME="$HOME/42/social"` (if not present)

---

### Task 1: Bootstrap `~/42/social/` content repo

**Files:**
- Create: `~/42/social/README.md`
- Create: `~/42/social/.gitignore`
- Create: `~/42/social/data-store.yaml`
- Create: `~/42/social/evolution-log.md`
- Create: `~/42/social/drafts/.gitkeep`, `~/42/social/posted/.gitkeep`
- Create: `~/42/social/voice/.gitkeep`, `~/42/social/sources/.gitkeep`

- [ ] **Step 1: Create directory tree**

Run:
```bash
mkdir -p ~/42/social/{voice,sources,drafts,posted}
touch ~/42/social/drafts/.gitkeep ~/42/social/posted/.gitkeep ~/42/social/voice/.gitkeep ~/42/social/sources/.gitkeep
```

Expected: tree created without errors. Verify with `ls -la ~/42/social/`.

- [ ] **Step 2: Write `~/42/social/README.md`**

```markdown
# ~/42/social — Louis's social content

Voice rules, drafts, published posts, and metrics for Louis's LinkedIn + X presence.

## Layout

- `voice/<platform>.md` — voice rules per platform (LinkedIn, X). Read by the `/post` skill, written by `/audit`.
- `sources/` — corpus the `/audit` skill reads. Pasted posts, LinkedIn export, X voice brief.
- `drafts/` — work-in-progress posts. Written by `/post`.
- `posted/` — drafts that have been published, with metrics in frontmatter. Moved here by `/log`.
- `data-store.yaml` — flat YAML, one row per posted item. Future pattern engine reads this.
- `evolution-log.md` — manual notes on rule changes ("changed hook rule X on YYYY-MM-DD because Y").

## How to use

Set `SOCIAL_HOME="$HOME/42/social"` in your shell rc. The three skills read from this env var.

1. **Bootstrap voice rules:** paste seed posts into `sources/linkedin-posts.md`, write a brief into `sources/x-brief.md`, then run `/audit linkedin` and `/audit x`.
2. **Write a post:** `/post linkedin` or `/post x` — interview-driven.
3. **Log after publishing:** `/log linkedin` or `/log x` — paste metrics manually.
4. **Periodic re-audit:** `/audit linkedin` after ~10 new posts to evolve rules.

Spec: `~/42/Markdowns2Teach/docs/superpowers/specs/2026-04-16-social-skills-design.md`
```

- [ ] **Step 3: Write `~/42/social/.gitignore`**

```
# Full LinkedIn export — too large + contains DMs, not for sharing
sources/linkedin-export/

# Local-only draft scratchpad (optional convention)
drafts/.local/
```

- [ ] **Step 4: Write `~/42/social/data-store.yaml`**

```yaml
# Posted items log. One entry per published post.
# Appended by /log. Read by future pattern engine.
posts: []
```

- [ ] **Step 5: Write `~/42/social/evolution-log.md`**

```markdown
# Evolution Log

Manual notes on why voice rules changed. Append-only. One line per change.

Format: `YYYY-MM-DD — <platform> — <rule changed> — <reason>`
```

- [ ] **Step 6: Init git repo, initial commit**

Run:
```bash
cd ~/42/social && git init && git add . && git commit -m "chore: bootstrap social content repo"
```

Expected: initial commit created with all bootstrapped files.

---

### Task 2: Seed source files

**Files:**
- Create: `~/42/social/sources/linkedin-posts.md`
- Create: `~/42/social/sources/x-brief.md`

- [ ] **Step 1: Write `~/42/social/sources/linkedin-posts.md`**

Seed with the 3 LinkedIn posts Louis pasted in conversation on 2026-04-16. Use this template:

```markdown
# LinkedIn seed posts

Seed corpus for `/audit linkedin`. Each post is a top-level section delimited by `---`.

When the full LinkedIn archive arrives (Shares.csv from the "Archive of your data" export), append posts here or replace this file with an extraction.

---

## Post: 2026-04-16 — Station F / Claude Code memory

**Impressions:** 563 · **Reactions:** 27 · **Comments:** 6

I just taught AI agents to founders at STATION F
The slide that sparked the most debate? How Claude Code's memory actually works, from the leaked source code.

Most people think AI agents "store everything." They don't. The best systems are brutally selective.

Here's what fills 1M tokens (slide 1) and how memory survives across sessions (slide 2):
🔹 The fixed prefix is tiny and KV-cached — same bytes every turn, ~10× cheaper.
🔹 The transcript is the largest segment. Three compaction stages keep it under 1M.
🔹 Memory is an index, not storage. MEMORY.md is just pointers — actual knowledge fetched on-demand.
🔹 Between sessions, a process called autoDream wakes up: a forked subagent that merges duplicates, removes contradictions, converts "yesterday" → "2026-04-14", and rebuilds the index.

The design philosophy applies to any long-running AI system:
→ Memory is a hint, not truth
→ If it's re-derivable, don't persist it
→ Staleness is first-class

In April 2026, six frontier models cluster within 1.3 points on SWE-bench. The model is no longer the moat — the harness is.

This was one piece of a 1-hour session covering agent anatomy, design patterns, frontier benchmarks, and why infrastructure is where the value migrated.

Merci Chloé Hervet for the invitation. And to all participants, the energy in the room was something else.

📎 Want the slides? Drop a comment.
#AI #Agents #ClaudeCode #StationF #ContextEngineering

---

## Post: 2026-04-02 (~2w) — 42 AI curriculum

**Impressions:** 4,433 · **Reactions:** 162 · **Comments:** 26 · **Reposts:** 8

I just designed three AI projects that will teach 25,000+ students worldwide how AI systems actually work under the hood.

I'm incredibly proud of this, and deeply grateful to everyone who made it happen.

42 has always built strong IT foundations: #algorithms, #oop, #networking, #systems. #AI projects already exist later in the curriculum, but until now, there was nothing in the Common Core, the foundational program every student goes through in their first two years.

That's what these projects change: giving students the systems-level understanding of AI early, so everything that comes after builds on solid ground.

Sophie Viger, CEO of 42 Network, asked me to create these projects from scratch. No existing baseline. I chose the topics, designed the progression, built the evaluation infrastructure, and wrote every spec.

I worked closely with a fantastic team at 42 who helped transcribe the subjects into LaTeX, organized beta tests with hundreds of students, and brought sharp pedagogical perspectives that made everything better. This was a deeply collaborative process and I'm grateful for every conversation.

Here's what every 42 student will now build, with no framework doing the heavy lifting:

🟡 𝗦𝘁𝗿𝘂𝗰𝘁𝘂𝗿𝗲𝗱 𝗼𝘂𝘁𝗽𝘂𝘁: constrained decoding at the token level, manipulating raw logits to guarantee 100% valid #JSON from a small model. This is how production AI systems actually work.

🔴 𝗥𝗔𝗚: a full Retrieval-Augmented Generation pipeline over a real codebase. Chunking, indexing, retrieval evaluation. The skill that makes LLMs useful in the real world.

🟢 𝗔𝘂𝘁𝗼𝗻𝗼𝗺𝗼𝘂𝘀 𝗰𝗼𝗱𝗲 𝗮𝗴𝗲𝗻𝘁𝘀: This is the one I'm most proud of.
#SWEBench is the benchmark where OpenAI, Anthropic, and Google prove their frontier models can do real software engineering. Each task is a real GitHub bug, with a Docker container and a test script, from an open source repo (django, sympy, matplotlib,...).

42 students will build the entire system that makes an agent solve these autonomously:
→ The reasoning loop (Think → Act → Observe)
→ A secure #sandbox (imports, builtins, network, filesystem, memory)
→ An #MCP tool server
→ Code extraction & context engineering
→ A dedicated chapter on #AI_Safety

These students won't just use AI agents. 𝗧𝗵𝗲𝘆'𝗹𝗹 𝗯𝗲 𝗮𝗯𝗹𝗲 𝘁𝗼 𝗯𝘂𝗶𝗹𝗱 𝗰𝘂𝘀𝘁𝗼𝗺 𝗼𝗻𝗲𝘀 𝗳𝗿𝗼𝗺 𝘁𝗵𝗲 𝗴𝗿𝗼𝘂𝗻𝗱 𝘂𝗽.

42 alumni already work across #AI, #ML, and #Data engineering. These projects give the next generation a head start on the systems side of AI, right from the foundations.

𝗜𝗳 𝘆𝗼𝘂'𝗿𝗲 𝗵𝗶𝗿𝗶𝗻𝗴 interns or apprentices, these students will come out with hands-on knowledge of structured output, #RAG pipelines, autonomous #agents, #sandbox security, and #MCP. That's rare at any career stage. Don't sleep on them.

Having a worldwide impact on AI education is something I never expected that early in my career. I don't take it lightly.

👇 R&D details and acknowledgements in comments.

---

## Post: 2026-03-26 (~3w) — Sorbonne IMT&E teaching wrap

**Impressions:** 1,176 · **Reactions:** 39 · **Comments:** 1

Merci aux étudiants du Master IMT&E de Paris 1 University of Paris I: Panthéon-Sorbonne pour ce post, et surtout pour ces 5 sessions de cours qui ont été un vrai plaisir.

Cette année, j'ai eu la chance d'enseigner "Machine Learning & Deep Tech" au M2 IMT&E : 5 × 3h pour donner à des profils entreprenariaux les clés pour décider intelligemment face à l'IA.

Le Master m'a laissé une liberté pédagogique totale, ce qui m'a permis de construire un cours ancré dans l'actualité la plus récente. Pas un catalogue figé, mais un contenu vivant mis à jour jusqu'à la veille de chaque session. L'objectif : que chaque slide reflète l'état de l'art réel, pas celui d'il y a 18 mois.

Le programme a couvert un spectre large :
→ Fondamentaux de l'IA générative et fonctionnement des LLMs
→ Embeddings, recherche sémantique et RAG
→ Agents autonomes et orchestration
→ Évaluation des modèles : benchmarks, métriques, pricing
→ Méthodologie projet IA, du MVP au déploiement
→ Business models, moats et études de cas (Klarna, Cursor, L'Oréal, Mistral…)
→ Régulation, éthique et EU AI Act
→ Un projet de groupe fil rouge : construire, évaluer et déployer un système de classification IA

La Session 4 évoquée dans leur post a été délocalisée à 42 Paris pour permettre l'accès à l'un de mes intervenants en fauteuil roulant. Merci à Grégoire Martinez de nous avoir ouvert le grand amphithéâtre pour l'occasion. Merci aussi à Maxime Jegat et Tanguy Auffret pour leurs retours terrain sans filtre sur l'entrepreneuriat.

Ce que je retiens surtout : des étudiants vifs, qui posent des questions pointues et challengent le contenu. Leur curiosité et leur exigence rendent l'exercice stimulant. Enseigner à des gens qui veulent vraiment comprendre, c'est un privilège.

Dernière session lundi prochain : présentations finales des projets, intervention de Juliette Lefay et clôture. Hâte de voir ce qu'ils ont construit.

Si le contenu vous intéresse, n'hésitez pas à me le dire en commentaire, je partagerai les slides avec plaisir.

#MachineLearning #DeepTech #IA #Enseignement #Paris1Sorbonne #MasterIMTE #Ecole42 #GenAI #LLM #RAG
```

- [ ] **Step 2: Write `~/42/social/sources/x-brief.md`**

```markdown
# X / Twitter — voice brief

No corpus yet. This brief is what `/audit x` uses to produce `voice/x.md` on first run.

## Target voice

- **Articulated, sharp insights.** A single point made with great clarity beats three points spread thin.
- **Focus over breadth.** Pick one small aspect and go deep with articulation.
- **Knowledge-share orientation.** Deliver the value I would have liked to find myself. Concrete, useful, immediately actionable when possible.

## Tone

- Confident but not preachy. Let the insight do the work.
- Direct, no fluff. X rewards economy.
- Technical without being elitist — the reader I'm writing for is sharp but maybe not at the SOTA frontier.

## Topics (what I'd write about)

- LLM systems engineering: context, memory, harness design, agent loops
- ML research → production reality gaps
- Concrete numbers from real work (ablation results, benchmarks I've run)
- Teaching frame: what tripped me up, what I wish someone had told me

## What I want to avoid

- Hot takes for engagement bait
- Vague "thoughts" without a specific claim
- Reposts of obvious content
- AI-tell patterns (em-dashes everywhere, "delve", "tapestry", "in conclusion")
- Hashtag spam (X doesn't reward it the way LinkedIn does)

## Format intuition (to validate during /audit)

- Single tweets > threads, unless the thread is genuinely structured
- 280-char tweets that stand alone
- Threads only when each tweet would be weaker in isolation
- Lead with the claim, support in follow-ups
```

- [ ] **Step 3: Commit**

Run:
```bash
cd ~/42/social && git add sources/ && git commit -m "feat: seed LinkedIn corpus (3 posts) and X voice brief"
```

---

### Task 3: Write `/audit` skill

**Files:**
- Create: `~/.claude/skills/audit/SKILL.md`

- [ ] **Step 1: Write the skill file**

```markdown
---
user-invocable: true
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion
description: Audit Louis's social posts to generate or update voice rules. Use when starting fresh (first-time audit) or after publishing ~10 new posts (re-audit). Triggered by /audit linkedin or /audit x. Reads $SOCIAL_HOME/sources/ + $SOCIAL_HOME/posted/, writes $SOCIAL_HOME/voice/<platform>.md, appends to $SOCIAL_HOME/evolution-log.md.
---

# /audit — Voice rules generator

## Trigger
`/audit linkedin` or `/audit x`

## Inputs

- `$SOCIAL_HOME` env var (fallback: `$HOME/42/social`)
- Platform arg: `linkedin` or `x`
- Corpus: `$SOCIAL_HOME/sources/<platform>-posts.md` + `$SOCIAL_HOME/posted/*-<platform>.md`
- For X first-run only: `$SOCIAL_HOME/sources/x-brief.md` (when no corpus exists)
- Existing rules (if any): `$SOCIAL_HOME/voice/<platform>.md`

## Workflow

### Step 1: Resolve paths

Read `$SOCIAL_HOME` from env. If unset, default to `$HOME/42/social` and warn.

Verify these exist:
- `$SOCIAL_HOME/sources/` (must exist, error otherwise)
- `$SOCIAL_HOME/voice/` (must exist, error otherwise)

### Step 2: Determine mode

| State | Mode |
|-------|------|
| `voice/<platform>.md` does not exist | **First-run** (generate from scratch) |
| `voice/<platform>.md` exists | **Re-audit** (propose diff) |

For X with empty corpus (`sources/x-posts.md` missing AND `posted/*-x.md` empty): use `sources/x-brief.md` as the only input.

### Step 3: Read corpus

- For LinkedIn: glob `$SOCIAL_HOME/sources/linkedin-posts.md` + `$SOCIAL_HOME/posted/*-linkedin.md`.
- For X: glob `$SOCIAL_HOME/sources/x-posts.md` (may not exist) + `$SOCIAL_HOME/posted/*-x.md`.
- If neither exists for X, fall back to `sources/x-brief.md`.

### Step 4: Analyse

Identify:
- **Hook patterns** — 3-5 named patterns with concrete examples from corpus (or from brief)
- **Tone** — confidence, formality, language conventions, mood
- **Structure** — typical post template (opener → … → close)
- **Format** — length range, bullet styles, signature moves (Unicode bold, emoji, etc.)
- **Vocabulary** — terms used freely, recurring expressions, banned patterns
- **Endings** — CTA styles, hashtag conventions
- **What to avoid** — generic patterns, AI tells, things absent from corpus that suggest deliberate omission

Write rules **prescriptively** ("Always use X") not descriptively ("tends to use X").

### Step 5: First-run path

Generate `$SOCIAL_HOME/voice/<platform>.md` using the template below. Present the full content for approval before writing. After approval, write the file.

Template sections (in order):

```markdown
# Voice rules — <Platform>

## Format
[Length range, structural patterns, bullet style, signature moves]

## Tone
[Confidence, formality, language conventions]

## Hook patterns
1. [Name] — [Example from corpus]
2. ...

## Structure
[Opener → ... → close]

## Vocabulary
[Terms used freely; recurring expressions]

## What to avoid
[Generic patterns, AI tells, banned words]

## Endings
[CTA styles, closing patterns]
```

### Step 6: Re-audit path

Read existing `voice/<platform>.md`. Compare against newly-extracted patterns. Present a **diff** of proposed changes — one section at a time via AskUserQuestion (accept / edit / reject).

Apply approved changes to the file.

### Step 7: Append to evolution log

Append one line per accepted change to `$SOCIAL_HOME/evolution-log.md`:

```
YYYY-MM-DD — <platform> — <rule changed> — <reason>
```

Today's date comes from the system. For first-run, the entry is:

```
YYYY-MM-DD — <platform> — initial voice rules generated from N source post(s)
```

### Step 8: Commit

Offer to commit:
```bash
cd $SOCIAL_HOME && git add voice/<platform>.md evolution-log.md && git commit -m "chore(voice): audit <platform> — <summary>"
```

Ask Louis before running git commands.

## Common failure modes

- **`$SOCIAL_HOME` unset**: fall back to `~/42/social`, warn user, suggest adding `export SOCIAL_HOME=$HOME/42/social` to `.zshrc`
- **No corpus and no brief**: stop, tell user to paste posts into `sources/<platform>-posts.md` or write `sources/<platform>-brief.md`
- **Existing rules look better than proposal**: respect Louis's "reject" — never overwrite without explicit accept

## Non-goals

- Does NOT scrape platforms
- Does NOT touch `data-store.yaml`
- Does NOT validate post content — that's `/post`'s job
```

- [ ] **Step 2: Validate frontmatter parses**

Run:
```bash
python3 -c "import yaml,re,sys; f=open('/home/ezalos/.claude/skills/audit/SKILL.md').read(); m=re.match(r'^---\n(.*?)\n---', f, re.S); print(yaml.safe_load(m.group(1)))"
```

Expected: dict with `user-invocable: True`, `allowed-tools` list, `description` string. No YAML errors.

- [ ] **Step 3: Commit skill files outside repo (skills live in ~/.claude/, not in this project)**

Skill files are not in a git repo by default. Skip git commit unless `~/.claude/` is tracked.

---

### Task 4: First-run `/audit linkedin`

This task is **executed by Louis interactively** in a fresh Claude Code session — it's not a writable task, it's a smoke test that the skill works.

- [ ] **Step 1: In a fresh Claude Code session, run `/audit linkedin`**

Expected:
- Skill reads `~/42/social/sources/linkedin-posts.md` (the 3 seed posts)
- Skill presents a full `voice/linkedin.md` proposal
- Louis approves or edits sections
- File written to `~/42/social/voice/linkedin.md`
- One entry appended to `~/42/social/evolution-log.md`

- [ ] **Step 2: Inspect the output**

Run: `cat ~/42/social/voice/linkedin.md`

Check that the file contains:
- The 3 hook patterns observed in seed posts (concrete-event-plus-claim, achievement-plus-scale, bold-Unicode-title)
- Reference to Unicode bold for section headers
- Reference to 🔹 / → bullet hierarchy
- Reference to named gratitude pattern
- "What to avoid" section including generic openers and AI tells

If the output is missing one of those, iterate: re-run `/audit linkedin` and provide feedback inline.

- [ ] **Step 3: Commit voice file**

```bash
cd ~/42/social && git add voice/linkedin.md evolution-log.md && git commit -m "feat(voice): linkedin v1 — audited from 3 seed posts"
```

---

### Task 5: First-run `/audit x`

- [ ] **Step 1: In a fresh Claude Code session, run `/audit x`**

Expected:
- Skill finds no corpus; falls back to `~/42/social/sources/x-brief.md`
- Skill generates `voice/x.md` from the brief, marking rules as "intuition, not corpus-validated"

- [ ] **Step 2: Inspect the output**

Run: `cat ~/42/social/voice/x.md`

Check:
- Sections present (Format, Tone, Hook patterns, Structure, Vocabulary, What to avoid, Endings)
- Hook patterns are tentative ("To validate" / "Pending corpus" notes are OK)
- Tone matches Louis's brief: sharp, articulated, focus over breadth

- [ ] **Step 3: Commit**

```bash
cd ~/42/social && git add voice/x.md evolution-log.md && git commit -m "feat(voice): x v1 — intuition-based from brief, awaiting corpus"
```

---

### Task 6: Write `/post` skill

**Files:**
- Create: `~/.claude/skills/post/SKILL.md`

- [ ] **Step 1: Write the skill file**

```markdown
---
user-invocable: true
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion
description: Interview-driven LinkedIn/X post writer in Louis's voice. Triggered by /post linkedin or /post x. Reads $SOCIAL_HOME/voice/<platform>.md, runs a 5-question interview, generates 5 hooks, drafts a full post, iterates with Louis, then writes to $SOCIAL_HOME/drafts/YYYY-MM-DD-slug-<platform>.md.
---

# /post — Post writer

## Trigger
`/post linkedin` or `/post x`

## Inputs

- `$SOCIAL_HOME` env var (fallback: `$HOME/42/social`)
- Platform arg: `linkedin` or `x`
- `$SOCIAL_HOME/voice/<platform>.md` (required — error if missing)

## Workflow

### Step 1: Resolve paths

Read `$SOCIAL_HOME`. Default to `$HOME/42/social` if unset (warn).

Verify `$SOCIAL_HOME/voice/<platform>.md` exists. If missing:
```
voice/<platform>.md not found. Run /audit <platform> first to generate it.
```
Stop.

### Step 2: Read voice rules

Read `$SOCIAL_HOME/voice/<platform>.md` fully. Internalise format, tone, hook patterns, structure, vocabulary, what-to-avoid, endings.

### Step 3: Interview

Ask one question at a time via AskUserQuestion. Capture answers.

| # | Field    | Options                                                   |
|---|----------|-----------------------------------------------------------|
| 1 | Goal     | Build authority / Inspire / Convert / Entertain / Document |
| 2 | Media    | Text-only / Image(s) / Carousel / Video                   |
| 3 | Message  | (free-text) raw idea — lesson, opinion, story, result    |
| 4 | Emotion  | Curiosity / Urgency / Agreement / Awe / Resonance         |
| 5 | Audience | (free-text) specifically who? not "everyone"             |

If Media = Video, ask a follow-up: "Paste transcript or key points (3-5 bullets)."

### Step 4: Generate 5 hooks

Apply hook patterns from `voice/<platform>.md` to the message + audience + emotion. Output:

```
Hook 1 — [Pattern name]
[Hook text — one or two lines]
Rationale: [Why this hook, tied to a specific pattern from voice rules]

Hook 2 — ...
```

Use AskUserQuestion: "Which hook do you want to build the post around?" (Options: 1, 2, 3, 4, 5, Regenerate, Edit one inline.)

If Regenerate: generate 5 new hooks (different patterns), repeat.
If Edit: ask which hook, accept the edited text, use that.

### Step 5: Draft the full post

Build the post around the chosen hook applying every voice rule from `voice/<platform>.md`:
- Length within the documented range
- Signature formatting (Unicode bold sections, bullet hierarchy, etc.)
- Tone matching documented confidence/language conventions
- Structure: opener (the hook) → context → bullets → reflection → gratitude (if applicable) → CTA → hashtags (LinkedIn) or nothing (X)
- Avoid every pattern in "What to avoid"

Output the full draft.

### Step 6: Iterate

Loop:
- User says "tighter" / "more numbers" / "rewrite the open" / "less formal" / etc. → apply targeted edit, show new draft.
- User says "ship it" → proceed to step 7.
- User says "scrap" → stop, no file written.

### Step 7: Slugify and save

Generate slug from message: lowercase, dash-separated, ≤6 words.

Filename: `$SOCIAL_HOME/drafts/<YYYY-MM-DD>-<slug>-<platform>.md`

Write with frontmatter:

```yaml
---
platform: <linkedin|x>
goal: <answer to Q1>
media: <answer to Q2>
audience: <answer to Q5>
hook_pattern: <name of pattern from voice rules>
status: draft
created: <ISO date>
---
```

Body: the final approved post text, ready to paste.

### Step 8: Output

Print:
```
Draft saved to: $SOCIAL_HOME/drafts/YYYY-MM-DD-slug-platform.md

----- POST -----
<full post text>
----- END -----

Copy-paste to <platform>. After publishing, run /log <platform> to capture metrics.
```

## Common failure modes

- **`voice/<platform>.md` missing**: stop, suggest `/audit <platform>` first
- **User scraps mid-iteration**: do not write a file
- **Slug collision with existing draft**: append `-2`, `-3`, etc.

## Non-goals

- Does NOT publish to any platform
- Does NOT touch `data-store.yaml` (that's `/log`)
- Does NOT modify voice rules (that's `/audit`)
```

- [ ] **Step 2: Validate frontmatter parses**

Run:
```bash
python3 -c "import yaml,re,sys; f=open('/home/ezalos/.claude/skills/post/SKILL.md').read(); m=re.match(r'^---\n(.*?)\n---', f, re.S); print(yaml.safe_load(m.group(1)))"
```

Expected: clean YAML dict, no errors.

---

### Task 7: Write `/log` skill

**Files:**
- Create: `~/.claude/skills/log/SKILL.md`

- [ ] **Step 1: Write the skill file**

```markdown
---
user-invocable: true
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion
description: Capture metrics for a published post and move it from drafts to posted. Triggered by /log linkedin or /log x. Prompts Louis for impressions/reactions/comments/reposts, appends a row to $SOCIAL_HOME/data-store.yaml, and moves the draft to $SOCIAL_HOME/posted/ with metrics in frontmatter.
---

# /log — Metric logger

## Trigger
`/log linkedin` or `/log x`

## Inputs

- `$SOCIAL_HOME` env var (fallback: `$HOME/42/social`)
- Platform arg: `linkedin` or `x`

## Workflow

### Step 1: Resolve paths

Read `$SOCIAL_HOME`. Default to `$HOME/42/social` if unset (warn).

Verify these exist:
- `$SOCIAL_HOME/drafts/`
- `$SOCIAL_HOME/posted/`
- `$SOCIAL_HOME/data-store.yaml`

### Step 2: Find unlogged drafts

Glob `$SOCIAL_HOME/drafts/*-<platform>.md`. These are candidates.

Present numbered list:
```
Unlogged <platform> drafts:
1. 2026-04-16-station-f-claude-memory-linkedin.md
2. 2026-04-15-rag-pipeline-thoughts-linkedin.md
3. (none / paste a URL for a post not in drafts)
```

Ask which one via AskUserQuestion.

If "none": prompt for raw post URL + create a stub `posted/` entry (no draft frontmatter to copy).

### Step 3: Collect metrics

Ask one at a time via AskUserQuestion:
1. Post URL (free-text, must look like https://... )
2. Impressions (integer)
3. Reactions total (integer, sum across like/celebrate/love/etc.)
4. Comments (integer)
5. Reposts (integer)
6. Notes (free-text, optional)

### Step 4: Compute derived fields

```
engagement_rate = (reactions + comments + reposts) / impressions
```

Round to 4 decimals. If impressions is 0, set engagement_rate to null.

### Step 5: Read draft frontmatter (if from drafts)

Extract: `hook_pattern`, `goal`, `media`, `audience`. Also count words in the body.

### Step 6: Append row to data-store.yaml

Read `$SOCIAL_HOME/data-store.yaml`. Parse YAML. Append to `posts` list:

```yaml
- id: <YYYY-MM-DD>-<slug>-<platform>
  platform: <linkedin|x>
  url: <post URL>
  posted_at: <ISO timestamp of logging — yes, logging time is a proxy when posted_at unknown>
  hook_pattern: <from draft frontmatter, or "unknown" if no draft>
  goal: <from draft frontmatter, or "unknown">
  media: <from draft frontmatter, or "unknown">
  audience: <from draft frontmatter, or "unknown">
  word_count: <count from body, or null if no draft>
  metrics:
    impressions: <int>
    reactions: <int>
    comments: <int>
    reposts: <int>
  engagement_rate: <float|null>
  notes: <string|null>
```

Write back. Validate the file still parses (re-read it and confirm yaml.safe_load succeeds).

### Step 7: Move draft to posted

If draft existed:
- Read draft frontmatter, add: `status: posted`, `url: <URL>`, `metrics: {impressions, reactions, comments, reposts}`, `engagement_rate: <float>`
- Move file: `mv $SOCIAL_HOME/drafts/<file> $SOCIAL_HOME/posted/<file>`
- Use Bash `mv`, do not use git mv unless asked.

If no draft (URL-only entry): create `$SOCIAL_HOME/posted/<id>.md` with frontmatter only.

### Step 8: Output

Print:
```
Logged: <id>
data-store.yaml updated (now N posts total)
Moved: drafts/<file> → posted/<file>
Engagement rate: X.XX%
```

### Step 9: Offer commit

Offer:
```bash
cd $SOCIAL_HOME && git add data-store.yaml posted/<file> && git rm drafts/<file> && git commit -m "feat(log): <id> — <impressions> imp, <reactions> rxn"
```

Wait for Louis to say yes.

## Common failure modes

- **`data-store.yaml` corrupted after write**: stop, show the error, do not commit
- **Impressions = 0**: still log, but set engagement_rate to null and add a note
- **URL collision**: append `-2`, `-3`, etc. to the id

## Non-goals

- Does NOT call any platform API
- Does NOT modify voice rules
- Does NOT generate posts (that's `/post`)
```

- [ ] **Step 2: Validate frontmatter parses**

Run:
```bash
python3 -c "import yaml,re,sys; f=open('/home/ezalos/.claude/skills/log/SKILL.md').read(); m=re.match(r'^---\n(.*?)\n---', f, re.S); print(yaml.safe_load(m.group(1)))"
```

Expected: clean YAML dict.

---

### Task 8: Set `$SOCIAL_HOME` in shell rc

**Files:**
- Modify: `~/.zshrc`

- [ ] **Step 1: Check if already set**

Run:
```bash
grep -n SOCIAL_HOME ~/.zshrc || echo "not set"
```

If "not set", continue. If already set, skip remaining steps for this task.

- [ ] **Step 2: Append export**

Run:
```bash
echo '' >> ~/.zshrc
echo '# Social content repo (used by /post, /log, /audit skills)' >> ~/.zshrc
echo 'export SOCIAL_HOME="$HOME/42/social"' >> ~/.zshrc
```

- [ ] **Step 3: Verify**

Run:
```bash
tail -3 ~/.zshrc
```

Expected: the 3 lines just appended.

- [ ] **Step 4: Reload (in a new shell)**

Louis opens a new terminal or runs `exec zsh`. Confirm:
```bash
echo $SOCIAL_HOME
```

Expected: `/home/ezalos/social`.

---

### Task 9: End-to-end smoke test

This is **executed by Louis interactively** to verify the system works.

- [ ] **Step 1: Run `/post linkedin` in a fresh session**

- Answer the 5 interview questions with a real or test message (e.g., "I just shipped a /post skill that writes LinkedIn posts in my voice").
- Pick a hook from the 5 generated.
- Iterate at least once ("tighter").
- Say "ship it".

Expected:
- File at `~/42/social/drafts/<YYYY-MM-DD>-<slug>-linkedin.md` with frontmatter and post body.
- The output post text is displayed in chat ready to paste.

- [ ] **Step 2: (Optional) Publish manually**

Louis copy-pastes to LinkedIn and publishes.

- [ ] **Step 3: Run `/log linkedin` in a fresh session**

- Pick the draft from the list.
- Enter the post URL.
- Enter impressions (e.g., 100 as a placeholder if not yet published — note this is a smoke test row).
- Enter reactions, comments, reposts.
- Optional notes.

Expected:
- `~/42/social/data-store.yaml` now contains one entry under `posts:`.
- `~/42/social/drafts/<file>` no longer exists.
- `~/42/social/posted/<file>` exists with metrics in frontmatter.

- [ ] **Step 4: Validate data-store.yaml**

Run:
```bash
yq '.posts | length' ~/42/social/data-store.yaml
```

Expected: `1` (or higher if more posts already logged).

Run:
```bash
yq '.posts[0]' ~/42/social/data-store.yaml
```

Expected: a clean YAML object with all required fields.

- [ ] **Step 5: Commit smoke-test state**

```bash
cd ~/42/social && git add . && git commit -m "feat: end-to-end smoke test — 1 draft → posted with metrics"
```

---

## Self-Review (after writing the plan above)

**Spec coverage:**
- ✅ `~/42/social/` content repo (Task 1)
- ✅ Seed sources — LinkedIn posts + X brief (Task 2)
- ✅ `/audit` skill (Task 3)
- ✅ `/post` skill (Task 6)
- ✅ `/log` skill (Task 7)
- ✅ Manual publication preserved (no platform API calls in any skill)
- ✅ Manual metric entry via /log
- ✅ Voice rules as versioned files (committed to git in Task 4-5)
- ✅ Data store in flat YAML (Task 7 spec)
- ✅ Evolution log appended by /audit (Task 3 spec)
- ✅ Smoke test end-to-end (Task 9)
- ✅ `$SOCIAL_HOME` in shell rc (Task 8)

**Gaps:** none flagged.

**Placeholder scan:** no TBDs, no "TODO later", every step has either a complete code block or a complete command with expected output.

**Type consistency:** field names in `data-store.yaml` schema are consistent between Task 7 and the spec. `hook_pattern`, `goal`, `media`, `audience`, `engagement_rate` match throughout.
